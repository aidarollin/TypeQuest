// extension/hud.js
// The floating HUD overlay is rendered directly inside content.js via Shadow DOM
// with inline styles to keep it self-contained and avoid CSP issues.
// This file is declared as a web-accessible resource in manifest.json so that
// content scripts can reference it by extension URL if needed in the future.
